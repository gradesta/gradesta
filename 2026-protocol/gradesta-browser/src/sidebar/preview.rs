//! Preview mode - displays current vertex content in the sidebar

use bevy_egui::egui;

/// Placeholder for preview rendering - the actual implementation
/// will be done later when integrating with main.rs
pub fn render_preview(
    _ui: &mut egui::Ui,
    _ctx: &egui::Context,
) {
    // This will be filled in during integration
    // For now, the preview is rendered inline in main.rs
}
